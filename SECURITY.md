# Security

Do not report real credentials, customer data, or other sensitive information in public issues.

Miranda intentionally treats domain boundaries as part of the application design. A language model behind the interface should not automatically turn a fashion-retrieval application into a general-purpose assistant.

Before publishing a fork or deployment, review model endpoints, secrets handling, upload validation, file parsing, logging, and any externally reachable interfaces appropriate to your environment.
